services:
  farkle-score:
    container_name: farkle-score
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    ports:
      - "8793:8787"
    volumes:
      # Host bind mount so the SQLite file is a plain file you can copy/back up.
      - ../data:/data
    restart: unless-stopped
